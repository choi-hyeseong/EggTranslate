package com.resoft.ocr_translation.repository;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TranslateRequestRepository extends JpaRepository<TranslateRequest, Long> {
}
